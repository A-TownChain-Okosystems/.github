# ATC-CHECK-AGENT-011 — Agent-Governance-Check (SCR-0064); Ausfuehrung: org_governance_scan.py + authorize.py
evaluation:
  fail_closed: true
  pass_when:
  - expect: push_workflow_file nur via Owner-Gate (GH013)
    type: no_destructive
evidence:
  kind:
  - api_response
  - file_content
  required: true
id: ATC-CHECK-AGENT-011
metadata:
  automation: MANUAL
  category: security
  level: MUST
  name: unauthorized-workflow-modification
  severity: P0
remediation:
  creates_finding: always
  required_on:
  - FAIL
requirement:
  expect: push_workflow_file nur via Owner-Gate (GH013)
  type: no_destructive
result:
  allowed:
  - PASS
  - FAIL
  - WARN
  - N/A
scope:
  level: repository
status: ACTIVE
version: 1.0.0
