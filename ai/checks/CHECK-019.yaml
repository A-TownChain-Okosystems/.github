# ATC-CHECK-019 — generiert von tools/gen_check_objects.py (Phase 2, SCR-0063)
alias: AGOV-CHECK-019
evaluation:
  fail_closed: true
  pass_when:
  - expect: 'least-privilege: kein write-all'
    type: workflow_permissions
evidence:
  kind:
  - api_response
  - file_content
  required: true
id: ATC-CHECK-019
metadata:
  automation: AUTO
  category: security
  level: MUST
  name: workflow-security-least-privilege-kein-write-all
  severity: P1
remediation:
  creates_finding: on_fail
  required_on:
  - FAIL
requirement:
  expect: 'least-privilege: kein write-all'
  type: workflow_permissions
result:
  allowed:
  - PASS
  - FAIL
  - WARN
  - N/A
scope:
  level: repository
status: ACTIVE
version: 1.0.0
