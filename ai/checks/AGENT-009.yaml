# ATC-CHECK-AGENT-009 — Agent-Governance-Check (SCR-0064); Ausfuehrung: org_governance_scan.py + authorize.py
evaluation:
  fail_closed: true
  pass_when:
  - expect: authorize.py chain scope ok
    type: no_destructive
evidence:
  kind:
  - api_response
  - file_content
  required: true
id: ATC-CHECK-AGENT-009
metadata:
  automation: MANUAL
  category: agent-auth
  level: MUST
  name: scope-verstoß-aktion-außerhalb-autorisiertem-scope
  severity: P0
remediation:
  creates_finding: always
  required_on:
  - FAIL
requirement:
  expect: authorize.py chain scope ok
  type: no_destructive
result:
  allowed:
  - PASS
  - FAIL
  - WARN
  - N/A
scope:
  level: repository
status: ACTIVE
version: 1.0.0
