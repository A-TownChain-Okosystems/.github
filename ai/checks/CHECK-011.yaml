# ATC-CHECK-011 — generiert von tools/gen_check_objects.py (Phase 2, SCR-0063)
alias: AGOV-CHECK-011
evaluation:
  fail_closed: true
  pass_when:
  - pattern: requirements.txt|poetry.lock|package-lock.json|Cargo.lock|uv.lock
    type: file_pattern
evidence:
  kind:
  - api_response
  - file_content
  required: true
id: ATC-CHECK-011
metadata:
  automation: AUTO
  category: quality
  level: SHOULD
  name: dependency-lockfile-vorhanden
  severity: P1
remediation:
  creates_finding: on_fail
  required_on:
  - FAIL
  - WARN
requirement:
  pattern: requirements.txt|poetry.lock|package-lock.json|Cargo.lock|uv.lock
  type: file_pattern
result:
  allowed:
  - PASS
  - FAIL
  - WARN
  - N/A
scope:
  level: repository
status: ACTIVE
version: 1.0.0
